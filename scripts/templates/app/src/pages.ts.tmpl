/**
 * {{APP_NAME}} page registrations
 *
 * Register your domain pages here. They will appear in the sidebar,
 * command palette, and page picker automatically.
 *
 * This file is loaded at app startup before the workspace renders.
 *
 * Usage:
 *   import { PageRegistry } from '@vastu/workspace';
 *
 *   PageRegistry.register({
 *     id: 'my-products',          // unique ID, used for cross-page navigation
 *     name: 'Products',           // display name in sidebar
 *     icon: 'IconBox',            // @tabler/icons-react icon name
 *     template: 'table-listing',  // page template
 *     section: 'PAGES',           // sidebar section
 *     order: 1,                   // sidebar position (lower = higher)
 *     config: {
 *       table: 'Product',         // Prisma model name
 *       columns: [
 *         { field: 'name', header: 'Name' },
 *       ],
 *     },
 *   });
 */

// import { PageRegistry } from '@vastu/workspace';

// ---------------------------------------------------------------------------
// Register your pages below
// ---------------------------------------------------------------------------

// TODO: Register your application pages here.
// See the example comment above and the Vastu docs for full PageRegistry API.
