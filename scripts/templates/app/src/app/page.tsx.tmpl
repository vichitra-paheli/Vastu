import { redirect } from 'next/navigation';

/**
 * Root page — redirects to the workspace.
 * The workspace is the primary entry point for {{APP_NAME}}.
 */
export default function RootPage() {
  redirect('/workspace');
}
