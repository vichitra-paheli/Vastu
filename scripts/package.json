{
  "name": "@vastu/scripts",
  "version": "0.0.0",
  "private": true,
  "description": "Vastu CLI scripts and scaffolding tools",
  "scripts": {
    "build": "tsc --noEmit",
    "lint": "eslint . --ext .ts --max-warnings 0",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "@vitest/coverage-v8": "^2.1.9",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
